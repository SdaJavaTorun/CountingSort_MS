package com.sdajava.countingsort;

/**
 * Created by User on 2017-03-17.
 */
public class Notes {
// pętla zliczająca wszsytkie liczniki liczb od razu
/*int k = 0;
    while (k < tablica.length) {
        for (int i = 0; i < tablica.length; i++) {
            if (tablica[i] == tablica[k]) {
                count[i]++;
            }
        }
        k++;
   } */
}
